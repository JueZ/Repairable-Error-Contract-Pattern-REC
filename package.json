{
  "name": "repairable-error-contract-pattern",
  "version": "0.1.0",
  "private": true,
  "description": "Draft pattern and JSON Schema profile for safe, structured, repairable API error responses.",
  "type": "module",
  "scripts": {
    "test": "node tests/validate-examples.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
